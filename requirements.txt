# FairBet Lab — dependencias Python
#
# Instalación local:  pip install -r requirements.txt

# Framework web y API
Django>=5.0,<6.0
djangorestframework>=3.15,<4.0

# Base de datos
psycopg2-binary>=2.9,<3.0

# Redis, tareas async y WebSockets
redis>=5.0,<6.0
celery>=5.4,<6.0
django-celery-beat>=2.6,<3.0
channels>=4.1,<5.0
channels-redis>=4.2,<5.0
daphne>=4.1,<5.0

# Configuración
python-decouple>=3.8,<4.0

# Servidor WSGI (despliegue; desarrollo usa runserver)
gunicorn>=22.0,<24.0

# Tests
hypothesis>=6.100,<7.0
pytest>=8.0,<9.0
pytest-django>=4.8,<5.0
pytest-cov>=5.0,<6.0
